tup=("a",10,-1,"Hello")
print("tuples and lists are similar but tuplease are immutable : We can' edit/add items in them")

print(tup[0])
print(tup[0:2])
print(type(tup))
print(dir(tup))
print ("We can check help by help(tup) or help(tup.count) etc")
